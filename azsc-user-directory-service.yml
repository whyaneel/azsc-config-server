spring:
  main:
    banner-mode: off
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    initialization-mode: always
    url: jdbc:postgresql://azsc-postgres-server.postgres.database.azure.com:5432/azsc-demo
    username: postgres@azsc-postgres-server
    password: Im4mPalo#
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG